services:
  - type: web
    name: chatbot-cursos
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SPREADSHEET_ID
        sync: false
      - key: SHEET_RANGE
        value: Cursos!A:J
      - key: GOOGLE_SERVICE_ACCOUNT_JSON
        sync: false
